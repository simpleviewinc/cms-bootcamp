$(document).ready(function(){
    // This is the submit button that sends the form data off to it's propper destination via a post request 
    $('#submit').click(function (e) { 
        e.preventDefault();
        // console.log(`Submit Button Click`);
        // location.href = "/login/";
    });

});


